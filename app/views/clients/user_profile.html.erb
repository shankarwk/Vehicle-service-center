<div class="container add">


    <% if @shop_owner%>
     <%@shop_owner.each do |i|%>
      <%= i.shop_owner%> <br>
      <%= i.shop_name%> <br>
      <%= i.location%>  <br>
      <%= i.address%>  <br>

      <% a = ServiceType.where(service_center_id:i.id)%>
        <% a.each do |t|%>
         Type of services:<%= t.name%> <br>
         Cost of service:<%=t.cost%>
        <br>
      <%end%>
      <!--  -->
      <% a = ServiceCenter.find(i.id)%>
      <% a.slots.all.each do |j|%>
       <%=j.name%> 
       <%=j.status%><br>
      <%end%>
      <!--  -->

      <%=link_to "all _request" , request_to_owner_path(i.id)%>
     <p>-----------------------------------------------------------------------</p>
  <%end%>
  <%else%>
    ftgyhjk
  <%end%>
  </div>